using Microsoft.AspNetCore.WebUtilities;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Diagnostics;

namespace HemaDrillBook.Tests
{
    [TestClass]
    public class MiscTests : TestBase
    {
        //[TestMethod]
        //public void ScratchTest()
        //{
        //    var uri1 = new Uri("http://test.com?a=1,2,3&a=4,5&b=7,8");
        //    var dict = QueryHelpers.ParseQuery(uri1.Query);
        //    foreach (var item in dict)
        //    {
        //        Debug.WriteLine($"Dictionary Key {item.Key} has {item.Value.Count} items");
        //        foreach (var part in item.Value)
        //            Debug.WriteLine("\t" + part);
        //    }
        //}
    }
}
